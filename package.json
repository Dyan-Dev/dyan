{
  "name": "dyan",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@8.15.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm turbo run dev",
    "build": "pnpm turbo run build",
    "lint": "pnpm turbo run lint",
    "build:codegen": "pnpm --filter=codegen build"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
